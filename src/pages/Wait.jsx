import Header from '../components/Header'

const Wait = ({ gameDate, setGameData, user, setPhase }) => {
    return (
        <div>
            <Header />
            <div>Wait</div>
        </div>
    )
}
export default Wait