import Header from '../components/Header'

const Wait = ({ gameData, setGameData, user, setPhase }) => {
    return (
        <div>
            <Header />
            <div>Wait</div>
        </div>
    )
}
export default Wait